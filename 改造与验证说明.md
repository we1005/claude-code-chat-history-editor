# Claude 首版改造与验证说明

本次范围由用户确认：先完成 Claude Code，Codex 后续再接入。工程基于 es6kr/claude-code-sessions 的 `39f3c7dea78acd38e7127563217d8a332b64bb5d` 快照。

## 已实现

- 新建版本化字段编辑服务和 HTTP API；Web 选择真实 JSON 路径对应的文本字段。
- 重复 UUID 与读取器一致，编辑最后一条；早期重复记录保留。
- 使用 jsonc-parser 的字符串 token 偏移替换，避免整文件 JSON.parse/stringify 破坏字节、未知数字或空白布局。
- 备份、文件修订校验、编辑器独占锁、临时文件同步、同目录替换和复读验证。
- 恢复旧字段而不是恢复整个旧文件；校验备份内容与目标记录结构，保留后来追加和其他字段编辑。
- 支持正文、thinking、嵌套工具结果文本；混合消息逐块显示，并提供可用的编辑入口。
- 草稿冲突保护、未保存提示、Markdown 安全预览、原文查看、会话 ID 复制和键盘焦点约束。
- 一键启动及隔离演示模式；项目运行时固定 Node 24.15.0，满足上游 jsdom 依赖的最低版本。

## 与上游不同的两个兼容行为

- 字符串 content 保存后仍是字符串，不再强制转成数组。
- 没有已有文本字段的结构不会被隐式加一个 text block。只有工具调用或图片时不提供这样的写操作。

旧 `updateMessageContent` 兼容入口现使用可靠保存服务和最后一个 UUID，但它没有外部调用方的旧版本参数，无法判断调用方长期持有的草稿是否过时。Web 使用新版本化 API，不依赖该兼容入口。

## 验证方法

本机实际结果（2026-09-15）：Core 29 个测试文件、524 项测试通过；Web 8 个测试文件、129 项测试通过；新增编辑器的 6 个 Playwright 场景在开发服务与生产构建中均通过。Core typecheck 通过，Svelte 检查无错误并保留一项上游 context 初始化警告。Core/Web 构建通过。

- Core 的新增文件操作测试全部使用临时文件；覆盖第二个文本块、重复 UUID、CRLF/BOM、大整数、工具结果、签名保留、空文本、并发/stale、备份损坏、模式权限及字段恢复。
- 修正了两项依赖运行机器 HOME 可逆 slug 的上游测试：Windows 大小写用确定样例；真实路径恢复使用已注册项目元数据，而不是假设短横线可逆。
- Web 的 Playwright 测试使用 `.editor-test/` 下隔离的 HOME 与合成 JSONL，通过真实 API 保存后直接比较磁盘内容，不只是检查成功提示。
- 同时检查开发服务和生产构建的关键保存/恢复流程。
- `./start.sh --demo --port 5180` 已实际启动并验证新编辑 API；修复了 `npm_execpath` 指向原生 pnpm 二进制时不能当 JavaScript 执行的兼容问题。

未使用真实用户会话进行自动写入，未调用模型，未自动验证正式 Claude CLI 的冷恢复及下一请求内容。不能把文件写回成功等同于所有客户端版本的恢复保证。

## 已知边界

- 官方 Claude 不遵守本工具的保存锁；与外部进程之间仍存在版本复查和替换的时间窗口。应先关闭目标会话。
- 不重算压缩摘要，不生成新 reasoning 签名。
- 自动恢复按已记录的字段路径进行；结构/元数据变化会拒绝，避免盲目把旧字段套到另一结构。
- 新编辑流程严格拒绝损坏 JSONL，不静默清理坏行；上游管理功能不是本次全部重写的对象。
- POSIX 文件权限会保留；平台 ACL、扩展属性及非 POSIX 文件语义尚未建立完整兼容矩阵。
- 上游 Core 同时导出浏览器工具和 Node 工具，构建可能出现 Node 模块 externalized 提示；以生产浏览器和 API 验证为准，不将构建提示当作完整功能证明。

## 发布

目标仓库：`we1005/claude-code-chat-history-editor`，公开，MIT。保留上游历史与许可证。本地参考仓库、研究材料、真实 JSONL 和备份不属于应用发布内容。上游自动 npm/VSIX 发布工作流已归档，启用的 CI 仅执行本项目检查。
